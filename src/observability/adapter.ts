/**
 * Observability adapter interface
 * Re-exported from core types for convenience
 */

export type {
  ObservabilityAdapter,
  Metric,
  RequestContext,
  ResponseContext,
  ErrorContext,
} from "../core/types.js";

